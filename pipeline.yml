name: Example workflow with OX Security Scan
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, reopened, synchronize]
    branches:
      - main
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Run OX Security Scan to check for vulnerabilities
        with:
          ox_api_key: ox_s9THdCBp0ZJ7Vf4nVSKykXi1Q1Ierrk5mPSj
          ox_host_url: https://dev-api.ox.security/api/github-app/webhooks
        uses: oxsecurity/ox-security-scan@main
